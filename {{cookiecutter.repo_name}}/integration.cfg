[buildout]
extends              = buildout.cfg
parts               += uwsgi
                       monit
                       nginx

[VARS]
SETTINGS             = settings_integration

[Gestus-client]
website_env          = integration
